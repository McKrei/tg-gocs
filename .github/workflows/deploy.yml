name: Deploy Bot to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /root/tg-gocs
            git pull origin main
            make up

      - name: Send Telegram Success Notification
        if: success()
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          COMMIT_AUTHOR: ${{ github.event.head_commit.author.name }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          CLEAN_MSG=$(echo "$COMMIT_MSG" | head -n 1 | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
          TEXT="🚀 <b>Деплой tg-gocs успешно завершен!</b>

<b>Коммит:</b> <a href='https://github.com/$REPO/commit/$SHA'>$CLEAN_MSG</a>
<b>Автор:</b> $COMMIT_AUTHOR"
          
          curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
            --data-urlencode "chat_id=$CHAT_ID" \
            --data-urlencode "parse_mode=HTML" \
            --data-urlencode "text=$TEXT" \
            --data-urlencode "disable_web_page_preview=true"

      - name: Send Telegram Failure Notification
        if: failure()
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          COMMIT_AUTHOR: ${{ github.event.head_commit.author.name }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          CLEAN_MSG=$(echo "$COMMIT_MSG" | head -n 1 | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
          TEXT="❌ <b>Ошибка деплоя tg-gocs!</b>

<b>Коммит:</b> <a href='https://github.com/$REPO/commit/$SHA'>$CLEAN_MSG</a>
<b>Автор:</b> $COMMIT_AUTHOR"
          
          curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
            --data-urlencode "chat_id=$CHAT_ID" \
            --data-urlencode "parse_mode=HTML" \
            --data-urlencode "text=$TEXT" \
            --data-urlencode "disable_web_page_preview=true"
